# top-webpack-boilerplate
webpack boilerplate with webpack-merge from The odin project

For ESLint - refer to eslint get started quick config - npm init @eslint/config@latest
(depends on the project if you have Typescript, other env variables and setups etc.)
